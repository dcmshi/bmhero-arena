# Handoff — 2026-07-30

Supersedes `HANDOFF-2026-07-27.md`. Written for a fresh session: read `CLAUDE.md`
first, then this. RE detail lives in the integration notes — **§8.18** (the
walker re-asserts its anim every frame), **§8.22** (action poses from the asset
table), **§8.23** (the poses now ANIMATE — the walker gate), **§8.24** (the
explosion visual — bomb pool doubles as blast pool), **§8.26** (the
single-player oracle and the gates it drives), **§8.20** (hazard suppression),
**§8.21** (HUD reuse), **§8.13** (render-drive + the blast-actor ceiling).

## Where things are

| repo | branch | state |
|---|---|---|
| `bmhero-arena` (canonical) | `main` | **local, unpushed**; GATE GREEN, sim **v18** hash `04e8af49` |
| `BMHeroRecomp` (fork) | `master` | **local, unpushed**; oracle-gate 17/17 GREEN |

**2026-08-02 addendum 8 — ORACLE 2.0: the whole-timeline differ (§8.35).**
Checks 1–16 are sixteen bespoke assertions, one per bug somebody already
noticed; **check 17** compares the arena's ENTIRE anim timeline against
vanilla's, verb by verb, so a wrong clip in a covered verb goes red without
anyone having to spot it first. Both scripted boots now run off verb tables
in `src/main/verb_script.h` (ticks; the ×2 poll conversion lives in exactly
one function), each row emitting a marker — `[oracle] phase=` on the vanilla
side, `[verb]` on the battle side — so a verb NAME joins the two boots, and
`[animrun]` carries the arena's full clip stream (the old `[anim]` burst caps
at 28 lines; mode 13 logs a 111-tick run). `tools\anim-diff.ps1` RLE-compares
the 10 shared verbs within ±3 frames; vanilla itself reproduced bit-for-bit
across three samples on one machine and one build (a strong signal, not a
proof), so the tolerance is all arena-side. Four verbs fail today
and live in `tools\oracle\known-divergences.json` with a one-line reason each
— the register is read BOTH ways: an unregistered FAIL fails the gate, and a
registered verb that PASSES also fails it, asking for the entry to be removed,
because a mute nobody ever clears is the vacuous green the whole instrument
exists to prevent. Three of the four are real bugs it caught on its first run
and are **backlog for the next feel round**: `setR2` stands on clip 41 where
vanilla idles on 0, `jumpon`'s jump ascent holds ~4 ticks against vanilla's
~34, and `relairB` skips vanilla's post-toss `[30,10]+[8,6]` (clip 30 is
information the scalar goldens never held). Falsifiability proven on the real
gate: `$env:ARENA_CARRY_WALK_ANIM='26'` makes `carrywalk` an unregistered FAIL
and takes 17 red (11 and 15 too); clearing it returns 17/17. Still sim v18,
hash `04e8af49` — this slice changed no gameplay.

**2026-08-01 addendum 7 — FEEL ROUND 11 (§8.34).** The hand bomb rode the
charge-run: the hide keyed on the windmill CLIP (26) and round 10's new
charge-run clip (28) joined the family unnoticed — the hide now keys on the
golden TIMER (62), the same state that selects the charged clips. Lesson:
key family-wide rules on the STATE, not on one clip. And the midair verbs
got measured: vanilla jumps while carrying on its OWN clip (20), the midair
release is a quick 3-frame toss (21) — NOT the grounded 10-frame lean (29)
that round 10 fired on every release and which read as "pushing bomberman
up" (vanilla `air_throw_y_rise 0.0`: no impulse exists; it was pure clip).
The airset jump-handback is generalized to all expiring midair poses.
oracle-gate is 16 checks. Still sim v18 — rounds 9–11 were entirely
render/bridge parity.

**2026-08-01 addendum 6 — FEEL ROUND 10 (§8.33).** Two round-9 leftovers,
still zero sim changes. Charged movement froze the feet (the windmill 26 has
static legs) — vanilla measures a distinct **charge-run clip 28**
(`windup_walk_anim_idx`, moves=true), now the driver's charged-moving
branch. And "sometimes the throw animation doesn't play": the walker's
one-shot throw trigger can die on the carry window's closing frame, and
nothing re-asserts — the bridge drives throw 29×10f on the HELD→AIRBORNE
edge (vanilla's spread release measures as the same clip 29). **Trap:** the
spread fan fills the game's ENTIRE bomb pool [2..5] and a set before it
clears spawns NOTHING (silent `Get_InactiveObject` failure) — one oracle run
measured its stand-on-bomb segment over empty floor and the goldens
"reproduced" vacuously; `[oracle-bomb]` going silent was the tell.
oracle-gate is 15 checks (release plays throw clip; charge-run clip while
moving, windmill absent from `[carryw]`).

**2026-08-01 addendum 5 — FEEL ROUND 9 (§8.32): the carry was never really
working.** Four reports, four fixes, ZERO sim changes (hash `04e8af49`
holds, no repin). (a) Feet frozen while carrying: the battle sweep kills the
walker's own carry bomb (`gObjects[2..5]`) every frame, so its carry state
machine re-triggered the pickup clip forever (41 pinned at frame 2) — the
bridge now DRIVES the carry clips through the pose window (idle 14 / walk 17
/ windup 26 @ 62f, all vanilla goldens; rolling 2-frame window that yields
to edge poses and TUMBLE). (b) The "invisible box" on laid bombs: the
airborne-only Y-drive handed Pos.y back to the walker's grounding on sim
landing, and its GROUND scan (unlike the push scan, `damageState` does not
blind it) stands on the door-class bomb actors at floor+210 — Pos.y is now
sim-driven ALWAYS. (c) The oracle's verdict on standing on bombs: vanilla
gives NO vertical support (`bomb_stand_supported false`, lift 0.0, resolved
to the floor 30u beside the bomb) — the user expected bomb-height standing;
if that's wanted it is a DESIGN choice (sim v19 + golden override), not
parity. (d) Air sets never latched a pose (FREE→FALLING was invisible to the
FREE→SETTLED latch) — now plays airset 51×8f, then hands back to the golden
JUMP clip (6) for the rest of the arc (mid-air, nothing else re-triggers —
the gate caught the 8f clip persisting 26f); and the charge-hide keys on
the walker's LIVE windup clip (26, latched per-frame) instead of the
120-tick sim timer, killing the ~58-tick both-visible overlap. **Traps
paid for:** the input POLL clock runs 2:1 against frames AND ticks (two
independent measurements; a +240-poll window in frame units overshot the
whole phase and returned a null golden); and the soak harness's flat 10s
post-PASS dwell silently truncated mode 13's ~20s choreography (-Expect now
waits, bounded). oracle-gate is 13 checks; goldens gained carry/windup/
stand/set-offset fields; `[oracle-player]` (slot 0xFE) and unthrottled
`[oracle] frame` are new channels.

**2026-08-01 addendum 4 — FEEL ROUND 6 (§8.29).** Held bombs render at the
HANDS (vanilla: feet+50, measured from the oracle's holdB window; was
head+lift). And the big one: the walker's own jump is a fixed MINI-HOP
(hold-independent, ~87 units/10 frames) while the sim's arc runs ~32 ticks —
the visible player landed ~20 ticks early, which is what made mid-air sets
look like they cut the jump. Player 0's Pos.y is now SIM-DRIVEN while
airborne (back to game grounding on landing); `arena_get_player_y` had been
an A1.2a stub returning 0 the whole time. 42-containment also restores
Vel.y. oracle-gate is 8 checks (check 8: driven Y reaches the sim apex).
Instrument lesson recorded in §8.29: sample at the WRITE site — the
entry-sampled channel could not see the drive and three probe rounds chased
a fix that was already working.

**2026-08-01 addendum 3 — FEEL ROUND 5 (fork `31c6f27`, sim v17, §8.28).**
Round 4's B-strip had silently removed the hold anim + multi-bomb windup +
its SOUND (all the walker's own carry actions) — **B passes through again**,
its game bombs staying sweep-hidden; only Z/R stripped, sim set = **R only**.
The 42-containment now RESTORES the last non-push state (idle-reset mid-jump
re-entered the jump on held A = the remaining "keeps rising"); 52 was never
the enemy and is uncontained. Sim v17: air-set bombs DROP from the hands
(`BSTATE_FALLING`) and settle — no floor teleport. Camera baked at the
user's pick: **1600/35** (2800/60 stays the inspection framing via env).
Backlog unchanged: vanilla kick slide-speed + air-set arc goldens; the
player overlay's solid-object scan RE (containment is still a stopgap).

**2026-08-01 addendum 2 — FEEL ROUND 4 (fork `6bb37cc`, sim v16, §8.27).** The
first oracle-gated boot found: throws GLIDING on open floor (sim v16:
`collide_static`'s floor clamp ate the contact before the floor check — y
joined the pushback compare; hash `242ce089`, new test (c) discriminates
where the blast is born); the air-set FLY-OFF (two mechanisms: the game
latches press EDGES at poll time so the old held-mask zeroing leaked every
edge — B/Z/R now stripped AT THE POLL with the sim reading a native latch,
and R maps to the sim set; plus the walker's own PUSH(42)/CARRY(52) reaction
to bomb actors overlapping it — five suppression channels measured and ruled
out, now CONTAINED same-frame, with the overlay's solid-object scan an open
RE item); and the kick clip cut at 10/18 frames (per-verb window,
`ARENA_KICK_POSE_FRAMES` default 18). "Kick feels slower" was almost
certainly the gliding throws — kick tuning never changed. oracle-gate is now
7 checks (kick full-clip length; mode-12 air-set no-push). Camera zoom/pitch
feedback is an open A/B with the user (play.ps1 -CamDist/-CamPitch). Backlog:
vanilla kick SLIDE SPEED + air-set fall arc as goldens.

**2026-08-01 addendum — the single-player ORACLE (fork `b37b038`→`5cfbc0a`,
§8.26).** `ARENA_ORACLE=1` boots the VANILLA campaign instrumented, mashes into
level 0, and scripts bomb verbs (`walk`/`stand`/`dropB`/`holdB`/`releaseB`/
`setR`/`walkoff`/`kickrun`/`jumpA`/`airsetR`/`DONE`) on the poll clock;
`tools\oracle.ps1` distils that run into `tools\oracle\goldens.json`, and
`tools\oracle-gate.ps1` asserts the ARENA matches — automatically, inside
`build.ps1 -Soak`. Three defaults flipped to what the game actually plays: set
pose **29 → 31** (29 is the THROW clip), kick pose **none → 33** (the kick
animation does exist), and `bomb_rest_lift` measured 30.0, confirming §8.25's
decomp constant exactly. The gate is proven falsifiable (`ARENA_SET_ANIM=3`
turns it red). **Two caveats before you debug a red one** (§8.26, "Two caveats"):
its first three checks share ONE mode-4 boot whose probe timing has silently
missed its set before, so suspect timing before poses when nothing pose-related
changed; and "set-pose plays once, full clip" is independent only while the
`set_anim_frames` golden (10) and the `ARENA_POSE_FRAMES` default (10) coincide.

**2026-07-31 addendum (fork `499fe64`):** the "thrown bombs slide" report was
the PREVIOUS build (v14 bounce-then-settle) — throw probe mode 11 proves
impact detonation on the current build (`[throw] t217 → [blastvis] t233`,
16 ticks = flight, not the 150-tick fuse). Set pose now plays ONLY WHEN
STANDING (a pose over a gliding body reads broken regardless of clip;
`ARENA_POSE_MOVING=1` restores always-pose). That exposed a probe bug:
mode 4's "standing set" had been landing inside the countdown and dropping —
the gate was green off the moving sets; timing fixed, and the moving pulses
now assert the inverse. Pose catalog: all 53 clips × 4 frames + `INDEX.html`
in fork `tools/anims/strips/`.

**Sim:** `TUNE_VERSION` **15**, hash **`cce00b99`** — v15: **thrown bombs
impact-detonate** (feel request; also the real game's behavior). An AIRBORNE
bomb explodes on contact with a player / ground bomb / wall / floor; the
bounce-then-settle path is gone (`TUNE_BOMB_RESTITUTION`/`_H_DAMP` now
unreferenced). New `test_throw_impact_detonates` (floor + in-air wall cases);
`test_spread_cap` redesigned on pre-placed set bombs. Work from `master` on
the fork.

## What happened this session (2026-07-30)

**The action poses animate (was: STATIC).** `func_8001BE6C` resets the anim
frame counter on *every* call, so §8.18's hold-by-retrigger pinned the clip at
frame 0 by construction. Fix (§8.23): `RECOMP_PATCH` on `func_8001C0EC` — the
single anim funnel the player overlay uses (all 69 machine-C anim calls; zero
direct `BE6C`/`C158`) — drops competing player-body anim writes while a pose
window is open; the pose is triggered once and the game's engine plays the clip.
The gate §8.18 had to retire as impossible (`-Rising 'frame=(\d+)'`) is
achievable again and green: set 41 runs 0→46 (+2/frame, full 24-frame window),
kick 32 runs 0→14. `arena-log.ps1` says `advanced (played)` where it said
`STATIC`. Duration gate and 5/5 boot soak also green on the committed build.

**Detonations now spawn the GAME'S OWN explosion (was: the bomb vanishes).**
§8.24: v1 (scaled bomb mesh as blast ball) survived one feel test ("reads as a
giant bomb, not a blast"); v2 calls `func_8007E76C(x,y,z,0)` — the exact
spawner the game bomb's fuse-out uses — on each sim blast birth. Dedicated
explosion pool `gObjects[6..13]` (no ceiling interplay), resident mesh, SFX
included, self-managed lifecycle. Screenshot-verified: fireball + smoke ring,
gone 2s later. Also fixed in passing: two blast exports were called with
implicit declarations (an f32-returning export read `$v0` instead of `$f0`).

**Feel round 1 (2026-07-30): "physics and feel seem good"** — clears
movement/respawn/skid/HUD. Round-1 nits all resolved same day (§8.25, fork
`4696b7d`): bombs rendered half-sunk → `BOMB_MESH_REST_LIFT` 30 (the game's
own rest height, 69AA0.c:359, raster-confirmed floor); set pose was WRONG and
LOOPING → default is now **29, the game's own drop clip, playing exactly
once** (window = clip length 10 frames; `ARENA_POSE_FRAMES` overrides); kick
pose default is now **none** (authentic — kick is bomb-side; 32/33 are crouch
clips, not kicks — front-view strips in fork `tools/anims/strips/`). New knob
`ARENA_CAM_YAW`, plus the facing trap it exposed: input co-rotates with
camera yaw, so orbiting a MOVING player never changes the view angle — front
shots come from mode 4's injected run (`+ ARENA_ANIM_SWEEP + follow cam`).

`CLAUDE_CODE_SUBAGENT_MODEL=opus` was added to `.claude/settings.json` (project
scope) — subagents default to Opus in this repo from the next session on.

## Open items, highest value first

### 1. FEEL-BOOT round 3 — SIGN-OFF ONLY (needs the human; the build is ready)

The objective half of this round is now **gated, not asked**: `oracle-gate`
(§8.26) asserts the set pose, its length, the bomb rest lift, the kick pose and
impact detonation against the vanilla game's own numbers on every
`build.ps1 -Soak`. Boot fork `5cfbc0a` in `build-rwdi` for what a gate cannot
judge — the goldens' own `no_oracle` list:

- **camera framing**, **explosion look** (fireball + smoke ring + SFX, §8.24),
  and **fun** — does the whole thing play well;
- a sanity glance that the gated things look right on screen too (set = 31
  played once, kick = 33 on contact, bombs resting ON the floor).

**CAVEAT before booting: clear stale env vars from earlier A/Bs** — they
override the new defaults silently:
`Remove-Item Env:\ARENA_SET_ANIM, Env:\ARENA_KICK_ANIM -ErrorAction SilentlyContinue`

If a pose STILL reads wrong, that is now a disagreement with the GAME, so treat
the oracle as the suspect instrument before picking by eye: rerun
`.\tools\oracle.ps1` and read the phase windows. The front-facing motion catalog
(all 53 clips × 4 frames, fork `tools/anims/strips/`) and
`ARENA_SET_ANIM`/`ARENA_KICK_ANIM`/`ARENA_POSE_FRAMES` remain for comparison —
but a hand-picked index that disagrees with a golden must move the golden, with
the run that produced it.

**Rerun `tools\oracle.ps1` whenever a new behaviour needs a reference** (a new
verb, a changed pose, an arena air-set). It refuses to overwrite differing
goldens without `-Force`, so drift surfaces as a diff. Goldens the arena does
not yet use are already recorded: drop (14 + throw), throw (29, 10 frames),
air-set (51, 8 frames).

### 2. Respawn is a TESTING ACCOMMODATION

`TUNE_RESPAWN_TICKS` exists so a feel tester isn't stuck watching after a
death. The shipping mode is stock-based elimination. Revisit before calling
the ruleset done — see the v14 commit message.

### 3. Real bomber mesh for puppets 1–3

Players 1–3 are still red bomb-mesh placeholders. The documented lead: player 0
animates in-arena, so valid anim data IS resident via the player path — start
by tracing `gPlayerObject`'s anim-instance bind. §8.5b.

### 4. Real battle arena, then A3

The Nitros room is a render stand-in. A map matching the sim's designed arena
resolves `arena_classic` geom + framing together. Then A3 online hardening
(rendezvous server, lobby codes, 4P WAN soak, desync surfacing).

## Traps that cost real time (carried forward — all still live)

1. **A gate can assert your own assumption and never fail.** Pair it with
   something you did not choose. (Counter-example done right: §8.23 restored
   the frame-rising gate precisely because the implementation now makes it
   *possible* to fail.)
2. **A call-graph search answers "is this used?", never "does this exist?".**
   For an asset, enumerate and render it.
3. **A failed build leaves the previous exe and the soak tests it happily.**
   Always confirm `BUILD OK` before trusting a probe result.
4. **Validate an instrument before trusting it.** §8.17.
5. **Measure the geometry, not the player.** §8.15.
6. **Probe timing is not tick timing.** Input poll runs ~45 ahead of the sim
   tick; the 180-tick countdown silently drops early buttons. §8.22.
7. **Literal `\n` in C string content** breaks the build. Use the Edit tool
   for C strings, not shell heredocs.
8. **MSYS2 gcc fails silently** without its own `bin` on PATH; the PS scripts
   prepend it.
9. **Export name collisions:** a `syms.ld` export must not share a name with
   the native implementation — follow the `arena_export_*` shim convention.
   Also: patch externs must match `lib/bmhero/include/functions.h` (it already
   declares most game funcs — a conflicting local extern is a build error).
   And an export called WITHOUT its `DECLARE_FUNC` compiles with only an
   implicit-declaration warning — for an f32-returning export the caller then
   reads `$v0` instead of `$f0` (garbage). Treat those warnings as errors
   (§8.24 found two).
10. **New `ARENA_AUTO_BATTLE` modes are gated in TWO places in `main.cpp`** —
    frontend-mash list and launcher list — or the boot hangs.
11. **`arena_bridge.log` is TRUNCATED per run** (`fopen "w"`), so a
    "lines since before the launch" delta scan reads either nothing or the
    wrong run. Delete the log before a launch and read the WHOLE file after.
    Related: world state varies run to run (absolute positions and object
    counts differ per boot) — key on markers and state sequences, never on a
    coordinate.

## Commands worth knowing

```powershell
# canonical
tools\gate.ps1                                   # everything CI runs, one command
tools\tune-report.ps1 -Compare base,turn=1456    # feel metrics sweep
tools\repin.ps1                                  # after an intentional sim change
tools\trace-diff.ps1 -A base -B friction=0.020   # first diverging tick

# fork
.\build.ps1 -Config rwdi -Soak 5                 # build + soak, fails unless green
.\tools\arena-log.ps1                            # summarise arena_bridge.log
.\tools\decomp-func.ps1 func_8001994C            # typed C for any function
.\tools\anim-contactsheet.ps1                    # 106 labelled pose PNGs -> tools\anims\
.\tools\rd-capture.ps1                           # RenderDoc frame, no keyboard

# gates
.\tools\oracle.ps1                                            # vanilla boot -> goldens
.\tools\oracle-gate.ps1                                       # arena vs goldens (in -Soak)
.\tools\arena-soak.ps1 -Mode 4  -Rising '\[animw\] \+\d+ idx=31 frame=(\d+)'  # set pose ANIMATES
.\tools\arena-soak.ps1 -Mode 10 -Rising '\[anim\] idx=33 frame=(\d+)'         # kick pose ANIMATES
.\tools\arena-soak.ps1 -AnimProbe                             # set pose still shown at +12
.\tools\arena-soak.ps1 -Mode 6 -Absent 'GUARD FIRED'          # must-never-happen
.\tools\arena-soak.ps1 -N 1 -Mode 7 -Expect '\[raster\] DONE'   # measure the floor

# runtime knobs, no rebuild
$env:ARENA_SET_ANIM='42'; $env:ARENA_KICK_ANIM='33'   # try the other pose
$env:ARENA_CAM_DIST='2400'; $env:ARENA_CAM_PITCH='30' # framing / angle
$env:ARENA_CAM_OFF='1'                                # A/B the camera on ONE binary
```

## One-time setup already done (don't redo)

- `.tools/decomp-venv` — python venv with splat's deps (+ `crunch64`, `pygfxd`,
  `colorama`, `pylibyaml`, missing from `requirements.txt`)
- `.tools/m2c` — cloned
- `.tools/llvm15` — portable LLVM 15.0.7 for the MIPS patches
- `lib/bmhero/baserom.z64`, `lib/bmhero/asm/` — staged, gitignored
