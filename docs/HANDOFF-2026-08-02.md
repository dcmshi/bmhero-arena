# Handoff — 2026-08-02

Supersedes `HANDOFF-2026-07-30.md` (kept for the round-4→11 addendum detail).
Written for a fresh session: read `CLAUDE.md` first, then this. RE detail lives
in the integration notes — **§8.32** (the carry driver + the ground-scan box),
**§8.33** (charge-run clip + the eaten throw), **§8.34** (midair verbs),
**§8.35** (oracle 2.0: verb tables + the whole-timeline differ), **§8.26**
(the original oracle), **§8.23** (the walker gate), **§8.18** (walker anim
re-assert — now known to be TRANSITION-ONLY, the root of three separate bugs).

## Where things are

| repo | branch | state |
|---|---|---|
| `bmhero-arena` (canonical) | `main` @ `ebbb041` | **pushed**; GATE GREEN, sim **v18** hash `04e8af49` |
| `BMHeroRecomp` (fork) | `master` @ `4e8ad23` | **pushed**; oracle-gate **17/17** + SOAK GREEN on that exact exe |

Feel rounds 9–11 are **user-verified** ("boot check looks good", 2026-08-01).
Rounds 9–11 and oracle 2.0 changed **zero sim code** — v18 has been pinned
since round 8. The fork submodule points at canonical `ebbb041`; both repos
push together whenever the pin moves (the fork won't clone otherwise).

**What exists now that didn't on 07-30:** the bridge *drives* the carry/throw/
midair clips from vanilla goldens (the walker's own carry is structurally
broken in battle — §8.32); player 0's Pos.y is sim-owned ALWAYS; and the gate
grew from 4 checks to **17**, the last of which diffs the arena's entire
per-verb anim timeline against vanilla's (`tools\anim-diff.ps1`,
`tools\oracle\timelines.json`, `known-divergences.json` — §8.35).

## Open items, priority order

1. **The three divergences the timeline differ caught on its first run**
   (tasks #29–31; each is registered in `known-divergences.json` and the gate
   goes red if you fix one WITHOUT removing its entry — that's the
   stale-exception rule working):
   - **setR2** (#29): after a ground set the arena idles on **clip 41** where
     vanilla idles on 0. 41 is the broken-carry pickup signature; likely the
     walker-never-re-asserts mechanism again — after OUR set pose window
     closes, nothing re-triggers idle.
   - **jumpon** (#30): the arena's jump-ascent clip holds ~4 ticks vs
     vanilla's ~34 — probably the walker's mini-hop clips running their own
     short arc while Pos.y flies the sim's.
   - **relairB** (#31): after the midair toss, vanilla plays a recovery
     sequence — **clip 30 ×10f then 8 ×6f** — the arena skips. Clip 30 is
     information the scalar goldens never held. Wire it into the
     `g_air_pose` handback chain.
2. **Instrument hardening** (small, high-leverage):
   - #32: check 17 floors verb COUNT but not DEPTH — assert the differ's
     passing-run subtotal (printed already; 8 today) so a shortened battle
     window can't shrink a verb to a passing prefix.
   - #33: derive the shared-verb set from `verb_script.h` in the gate and
     delete the hand-kept `$AD_MIN_VERBS = 10` (it decays: verbs added past
     ten are silently un-floored). Also records: the `-Expect '\[hitpose\]'`
     terminator swap was CONSIDERED and deferred — checks 9/13 need the blast
     to have happened.
3. **Design decision pending (user's call): standing on bombs.** The oracle
   measured vanilla giving **no vertical support** (`bomb_stand_supported
   false`; you land on the floor 30u beside it) and the arena now matches.
   The user originally expected bomb-height standing — if that's wanted it's
   a *design* choice: sim v19 + golden override, not parity.
4. **#24: grounded player↔settled-bomb pushout** to the vanilla 30u contact
   gap (golden `bomb_stand_xz_gap` already measured). Sim change → v19 +
   tests + repin. Pairs naturally with item 3 if that goes ahead.
5. **#18 (old): air-set fall-arc golden.**
6. **Deferred DESIGN item:** the differ deletes ≤3f/≤5f boundary residue
   (per-side caps) rather than COMPARING it — comparison would make the
   arena's 5-tick stick→locomotion latency visible, but puts carrywalk on
   the ±3 tolerance edge; blocked on arena-side boot-variance data (vanilla
   variance measured ≈0 over 3 samples; arena unmeasured).
7. **A1 polish (unchanged):** puppets 1–3 are still bomb-mesh placeholders
   (real bomber mesh, §8.5b lead); the arena is the Nitros stand-in, not a
   purpose-built battle map.
8. **RE backlog:** the player overlay's solid-object scan (root fix for the
   42-containment stopgap, §8.29) is still un-decompiled.
9. **Then A3** — online hardening (ROM-free): rendezvous server + lobby
   codes, host-relay fallback, 4P mesh WAN soak, desync surfacing,
   player-slot assignment.

Low-priority deferred minors (triaged by the final oracle-2.0 review; none
block anything): `verb_script.h` uses NULL with only `<stdint.h>` (add
`<stddef.h>` at the next rebuild); `carryB` fires 9 ticks after the 180-tick
countdown ends (comment the margin); the timeline emitter could assert
`sum(runs) <= frames`; malformed `known-divergences.json` throws instead of a
clean FAIL (fails closed); `oracle.ps1`'s `Write-Error`+`exit` style under
`$ErrorActionPreference=Stop` prints ugly (correct behavior).

## Traps that cost real time (new since 07-30 — older ones in that file)

- **The walker only re-asserts anims on its own state transitions**, not
  every frame (§8.18 as originally written oversold it). Three bugs from
  this: the airset clip persisting 26f, the eaten throw trigger, and clip 41
  idling after sets (#29). Anything the bridge triggers mid-air or over a
  closing pose window needs an explicit handback.
- **A clip-keyed rule breaks silently when a new clip joins the family.**
  The charge-hide keyed on windmill 26; charge-run 28 arrived and the hide
  didn't know. Key family-wide rules on the STATE that selects the family
  (the hide now keys on the golden 62-tick timer).
- **The input POLL clock runs 2:1 against frames AND sim ticks.** Now
  structural: choreography lives in TICK-unit verb tables
  (`src/main/verb_script.h`) and the ×2 conversion exists in exactly one
  function. Never hand-convert again.
- **The spread fan fills the game's whole bomb pool [2..5]**; a set before
  it clears spawns NOTHING (silent `Get_InactiveObject` failure). One oracle
  run measured its stand-on-bomb segment over empty floor and the goldens
  "reproduced" **vacuously** — `[oracle-bomb]` going silent was the tell. A
  green re-measurement is only as good as the scene it measured.
- **The ground scan is not the push scan**: `damageState = OBJ_DEATH` blinds
  the 21E10-family interaction scan but NOT the walker's ground scan (it
  stood on door-class bomb actors at floor+210). Assume nothing transfers
  between the two.
- **`arena_bridge.log` truncates on EVERY boot** (fopen "w"). Copy a
  measurement log aside *immediately*; `oracle.ps1 -FromLog <copy>` re-parses
  without a 3-minute boot.
- **Goldens "byte-identity" is field-level**: the `provenance` block
  re-stamps `fork_commit`/`date` on every extraction, so whole-file compare
  is red-by-construction across commits. `oracle.ps1` compares measured
  fields only (both PowerShell editions serialize numbers differently, too —
  the compare normalizes).
- **`timelines.json` property order is load-bearing** — the emitter walks it
  as the temporal chain to resolve carried clips. Never hand-edit or sort it;
  regenerate with `oracle.ps1 -Force`.
- **`verb_script.h` sharp edges:** a row with `start == 0` never fires its
  marker (the poll counter is 1-based), and two rows sharing a name silently
  collapse to the first. The stick signs are inverted on purpose (-1 drives
  +Z) and the goldens depend on them — the comment on `stick_y` explains.
- **Adding a shared verb** is a documented procedure now (§8.35): both
  tables → rebuild → `oracle.ps1 -Force` (a new vanilla marker SPLITS the
  neighbour's window, so the neighbour's timeline legitimately changes) →
  raise `$AD_MIN_VERBS` → keep the battle window prefix-shaped or use a
  battle-only name.

## Commands

```powershell
# canonical (sim) — unchanged loop
tools\tune-report.ps1 -Compare base,friction=0.020
tools\gate.ps1                                   # all suites + hash + metrics
tools\repin.ps1                                  # only with TUNE_VERSION bump

# fork — build + full gate
.\build.ps1 -Config rwdi -Soak 5                 # soak + oracle-gate 17/17
.\tools\oracle.ps1                               # vanilla boot -> goldens + timelines
.\tools\oracle.ps1 -FromLog <saved.log> -Force   # re-extract without a boot
.\tools\oracle-gate.ps1                          # 17 checks (~8 min, 5 boots)
.\tools\anim-diff.ps1 -ArenaLog arena_bridge.log # the timeline differ, standalone
.\tools\arena-soak.ps1 -N 1 -Mode 13 -TimeoutSec 130 -Expect '\[hitpose\]'  # the m13 probe
.\tools\play.ps1                                 # launch for a human boot (clean knobs)

# falsify the timeline gate (must go RED)
$env:ARENA_CARRY_WALK_ANIM='26'; .\tools\oracle-gate.ps1; Remove-Item Env:\ARENA_CARRY_WALK_ANIM
```

## One-time setup already done (don't redo)

- `.tools/decomp-venv` — python venv with splat's deps (+ `crunch64`,
  `pygfxd`, `colorama`, `pylibyaml`, missing from `requirements.txt`)
- `.tools/m2c` — cloned
- `.tools/llvm15` — portable LLVM 15.0.7 for the MIPS patches
- `lib/bmhero/baserom.z64`, `lib/bmhero/asm/` — staged, gitignored
